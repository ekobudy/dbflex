package test

/*
import (
	"testing"

	"github.com/eaciit/dbflex"
	"github.com/eaciit/toolkit"
)

func TestRegisterOrm(t *testing.T) {
	err = dbflex.RegisterModel(new(datamodel), toolkit.M{}.
		Set("tablename", "employees").
		Set("id", []string{"id"}))
	check(t, err, "unable to register model: %s", err.Error())
}

func TestModelGet(t *testing.T) {
	dm := new(datamodel)
	err := dbflex.Get(sess, dm, "EMP01")
	check(t, err, "unable to get data: %s", err.Error())

	dr := Get("EMP01")
	compare(dr.Level, dm.Level)
}

func TestModelGets(t *testing.T) {
	cursor, err = dbflex.Gets(sess, toolkit.M{}.
		Set("where", dbflex.NewFilter().Eq("level", "manager")).
		Set("order", []string{"id"}))
	check(t, err, "unable to get data: %s", err.Error())

	dr := Get("EMP01")
	compare(dr.Level, dm.Level)
}
*/
